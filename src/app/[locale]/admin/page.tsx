import AdminScreen from "@/components/AdminScreen";

// Sin generateMetadata a propósito: no es una página de cara al usuario, no
// hace falta un título/descripción pensados para compartir o indexar.
export default function AdminPage() {
  return <AdminScreen />;
}
